import os, time, sys\napi_key = os.environ.get("RENDER_API_KEY")\nservice_id = os.environ.get("RENDER_SERVICE_ID")\nif not api_key or not service_id:\n    print("⚠️ WARNING: Missing RENDER_API_KEY or RENDER_SERVICE_ID.")\n    print("Watchdog disabled — using external cron-job.org instead.")\n    while True:\n        time.sleep(3600)\nelse:\n    print(f"✅ Watchdog active for service {service_id}")
