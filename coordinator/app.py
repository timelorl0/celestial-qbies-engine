from datetime import datetime, timedelta\nfrom fastapi import FastAPI\nimport time\n\napp = FastAPI()\n\nstart_time = datetime.utcnow()\n\n@app.get("/health")\ndef health():\n    now = datetime.utcnow()\n    timestamp = now.strftime("%Y-%m-%d %H:%M:%S")\n    uptime = str(now - start_time).split(".")[0]\n    print(f"[{timestamp}] 🩺 Health check received from cron-job.org — uptime: {uptime}")\n    return {"ok": True, "version": "1.0", "status": "running", "time": timestamp, "uptime": uptime}\n\n@app.get("/uptime")\ndef get_uptime():\n    now = datetime.utcnow()\n    uptime = str(now - start_time).split(".")[0]\n    return {"uptime": uptime, "since": start_time.strftime("%Y-%m-%d %H:%M:%S UTC")}\n
